import { AppExceptionFilterFilter } from './app-exception-filter.filter';

describe('AppExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new AppExceptionFilterFilter()).toBeDefined();
  });
});
