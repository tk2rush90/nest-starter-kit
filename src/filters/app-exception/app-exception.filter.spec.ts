import { AppExceptionFilter } from './app-exception.filter';

describe('AppExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new AppExceptionFilter()).toBeDefined();
  });
});
